# Dr. Jekyll Action

This GitHub Action builds a Jekyll site and deploys it to GitHub Pages. It is designed to be used in a workflow that runs on the `main` branch, and it will only deploy the site if the build is successful.
